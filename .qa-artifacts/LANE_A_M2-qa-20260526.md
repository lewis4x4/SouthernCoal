# Lane A Milestone 2 — QA run

**Tester:** Cursor agent (automated + manual browser)  
**Date:** 2026-05-26  
**Build / commit:** `657bf8f` (uncommitted M2 spine changes on working tree)  
**Environment:** Local dev — `http://localhost:5174` (Vite; port 5173 was a different SCC-OS app)  
**User:** `wv-uat-sampler@invalid.scc.local` (field_sampler)

## Preconditions

| Item | Result |
|------|--------|
| M1 overlap (field user, WV UAT visits) | **Pass** — UAT visits on 2026-04-02 in Field Queue |
| Online load first | **Pass** — route date 2026-04-02 + visit `f00000c4-0004-4004-8004-000000000004` loaded online |
| Network disable | **Pass** — simulated via `navigator.onLine` + `offline` event (CDP `Emulation.setOffline` unavailable in IDE browser) |
| `audit_log` access | **Partial** — Supabase query returned 0 recent `field_outbound_*` rows for UAT sampler this session (no flush/block induced) |

## B1 — Durable offline route + visit context

| Step | Result | Notes |
|------|--------|-------|
| Online route + visit | **Pass** | Apr 2 route shows 4 stops; visit wizard loads (locked completed visit) |
| Offline route after save | **Pass** | After **Save route offline**: banner *Offline — saved route on this device* cites **localStorage / IndexedDB**; 4 stops visible; **FieldDataSyncBar** with Refresh |
| Reload + offline (same session) | **Partial** | Full navigation reset `navigator.onLine`; after re-offline + date `2026-04-02`: *Offline with no saved route* while `localStorage` route key was **null** (visit cache key still present). **Follow-up:** confirm route dual-cache on production `preview` + real airplane mode; IDB may hold route when LS empty |
| Offline visit (cached) | **Pass** | Visit reloads offline from per-visit cache; sync bar visible |
| Cold-start visit (no cache) | **Pass** | Unknown visit UUID offline → *Offline with no saved visit on this device…* + sync bar + Retry |

## B2 — Sync health

| Step | Result | Notes |
|------|--------|-------|
| Sync bar when offline | **Pass** | Route and visit show **Refresh field data from server** / stale-data copy (`#field-sync-health` region in snapshots) |
| Pending / blocked visibility | **Not exercised** | 0 outbound ops this session; no induced evidence failure |
| Diagnostic consistency | **N/A** | No queue block triggered |

## B3 — Reconnect flush ordering

| Step | Result | Notes |
|------|--------|-------|
| Live flush after offline work | **Not exercised** | No queued ops created in browser run |
| Code trace | **Pass** | `useFieldOps.flushOutboundIfOnline` uploads evidence **before** `processFieldOutboundQueue` (see `useFieldOps.ts` ~737–772) |

## B4 — Conflict hold

| Step | Result | Notes |
|------|--------|-------|
| Induced conflict | **Not exercised** in browser |
| Unit tests | **Pass** | `fieldOutboundQueue.test.ts` — conflict hold paths; 60 tests across field cache/queue/ops |

## B5 — Audit trail

| Step | Result | Notes |
|------|--------|-------|
| `audit_log` spot-check | **Inconclusive** | No `field_outbound_queue_flushed` / `_blocked` / `field_outbound_conflict_hold` rows in last 15 DB rows (no flush events this run) |
| Code | **Pass** | Actions defined in `useAuditLog.ts`; flush/blocked/conflict wired in `useFieldOps.ts` |

## Automated regression (same session)

```
npm run typecheck — pass
vitest: fieldVisitLocalCache, fieldRouteLocalCache, fieldOutboundQueue, useFieldOps — 60 tests pass
```

## Sign-off

**Milestone 2 accepted:** **Conditional** — core B1/B2 UX verified on dev; B3–B5 rely on code/tests + prior implementation. **Not accepted for formal staging closure** until:

1. **Production preview** QA per `docs/field-offline-qa.md` (PWA, real offline, multi-hour window).  
2. **Route cache survive hard refresh** validated (IDB route row present when LS cleared).  
3. One scripted run inducing **pending queue + flush + audit_log** rows (B2/B3/B5).

**Reason:** Dev-only offline simulation; route cache not observed in `localStorage` after full reload; no outbound flush audit rows generated this session.
